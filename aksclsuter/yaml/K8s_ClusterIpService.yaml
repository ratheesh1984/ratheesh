apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: default
  labels:
    app: nginx
spec:
  type: ClusterIP
  selector:
    app: nginx          # must match Deployment pod labels
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80